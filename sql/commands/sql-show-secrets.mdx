---
title: "SHOW SECRETS"
description: "Use the `SHOW SECRETS` command to list all secrets in a schema."
---

<Tip>
**PREMIUM FEATURE**

This is a premium feature. For a comprehensive overview of all premium features and their usage, please see [RisingWave premium features](/get-started/premium-features).
</Tip>

## Syntax

```sql
SHOW SECRETS [ FROM schema_name ] [ LIKE_expression ];
```

## Parameters

| Parameter or clause | Description                                                                                                                                                                               |
| :------------------ | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| _schema\_name_      | The schema of the secrets to be listed. The default schema is public.                                                                                                                     |
| LIKE\_expression    | Filters the output based on names by applying pattern matching. See details in [LIKE pattern matching expressions](/sql/functions/string#like-pattern-matching-expressions). |

## Output

The command returns the following column:

| Column | Description |
| :----- | :---------- |
| Name | The name of the secret, including the schema prefix (e.g., `public.secret_name`). |

## Example

```sql
SHOW SECRETS;
```

```bash
        Name
---------------------
 public.mysql_pwd
 public.kafka_token
(2 rows)
```

## Notes

Starting from a recent version, the `Name` column in the output includes the schema name as a prefix (e.g., `public.secret_name`). This helps distinguish secrets with the same name in different schemas when querying across multiple schemas in the search path.

## Related topics

<CardGroup>
  <Card
    title="Manage secrets"
    icon="key"
    iconType="solid"
    href="/operate/manage-secrets"
    horizontal
  >
    Learn about secret management
  </Card>
  <Card
    title="CREATE SECRET"
    icon="circle-plus"
    iconType="solid"
    href="/sql/commands/sql-create-secret"
    horizontal
  >
    Create a secret
  </Card>
  <Card
    title="DROP SECRET"
    icon="trash"
    iconType="solid"
    href="/sql/commands/sql-drop-secret"
    horizontal
  >
    Drop a secret
  </Card>
</CardGroup>
