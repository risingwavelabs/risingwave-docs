---
title: "Use dbt for data transformations"
description: "This guide provides instructions for using dbt to manage real-time data transformations in RisingWave."
sidebarTitle: dbt
---

## Install the plugin

To use dbt to manage data in RisingWave, you need to install the [dbt-risingwave](https://github.com/risingwavelabs/dbt-risingwave) plugin.

As a prerequisite, you need to have Python3 installed in your environment.

Run the following command to install the plugin:

```bash
python3 -m pip install dbt-risingwave
```

After the plugin is installed, run the following command to ensure `risingwave` shows up under plugins.

```bash
dbt --version
```

If you see something like below, the plugin is successfully installed:

```bash
Plugins:
...
  - risingwave: 1.6.0 - Up to date!
...
```

## Initiate a dbt project

Before you initial a dbt project, you need to ensure that RisingWave is installed and running. To learn about how to install and run RisingWave, see the topics under _Get started_.

You can initiate a [dbt project](https://docs.getdbt.com/docs/build/projects) by running the following command.

```bash
dbt init
```

It will ask you to enter a project name, choose the database you like to use (`risingwave`), and specify other database configurations such as hostname, port, user name, etc.

The default database configurations are:

* host: localhost
* port: 4566
* user: root
* database: dev

If you use different RisingWave configurations, please ensure that the configurations here match those in RisingWave.

## Define dbt models
The dbt models for managing data transformations in RisingWave are similar to typical dbt SQL models. The main differences are the materializations. We customized the materializations to fit the data processing model of RisingWave.

RisingWave accepts these [materializations](https://docs.getdbt.com/docs/build/materializations).

| Materializations       | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| :--------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| table                  | This materialization creates a table. To use this materialization, add `{{ config(materialized='table') }}` to your model SQL files.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| view                   | Create a view. To use this materialization, add `{{ config(materialized='view') }}` to your model SQL files.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ephemeral              | This materialization uses [common table expressions](/sql/query-syntax/with-clause) in RisingWave under the hood. To use this materialization, add `{{ config(materialized='ephemeral') }}` to your model SQL files.                                                                                                                                                                                                                                                                                                                                                                                                          |
| materializedview       | To be deprecated. It is available only for backward compatibility purposes. Use materialized\_view instead.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| materialized\_view     | Creates a [materialized view](/sql/commands/sql-create-mv). This materialization corresponds the incremental one in dbt. To use this materialization, add `{{ config(materialized='materialized_view') }} `to your model SQL files.                                                                                                                                                                                                                                                                                                                                                                                                   |
| incremental            | Use materialized\_view instead. Since RisingWave is designed to use materialized view to manage data transformation in an incremental way, you can just use the materialized\_view materilization.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| source                 | Defines a source. To use this materialization, add `{{ config(materialized='source') }}` to your model SQL files. You need to provide your create source statement as a whole in this model. See [Example model files](#example-model-files) for details.                                                                                                                                                                                                                                                                                                                                                                               |
| table\_with\_connector | Defines a table with connector settings. A table with connector settings is similar to a source. The difference is that a table object with connector settings persists raw streaming data in the source, while a source object does not. To use this materialization, add `{{ config(materialized='table\_with\_connector') }}` to your model SQL files. You need to provide your create table with connector statement as a whole in this model (see [Example model files](#example-model-files) for details). Because dbt table has its own semantics, RisingWave use table\_with\_connector to distinguish itself from a dbt table. |
| sink                   | Defines a sink. To use this materialization, add `{{ config(materialized='sink') }}` to your SQL files. You need to provide your create sink statement as a whole in this model. See [Example model files](#example-model-files) for details.                                                                                                                                                                                                                                                                                                                                                                                           |

To learn about how to define SQL models in general, see [SQL models](https://docs.getdbt.com/docs/build/sql-models).

To learn about how dbt works with RisingWave in practice, check out our [demo project](https://github.com/risingwavelabs/dbt_rw_nexmark), which uses dbt to manage Nexmark queries in RisingWave.

## Run dbt models

This step assumes that you have defined your dbt models.

Navigate to your project directory.

```bash
cd <your project name>
```

First, you can run `dbt debug` to check your connection to RisingWave.

```bash
dbt debug
```

If the connection is valid, you will see an "OK connection ok" message. In this case, you can run your models. Otherwise, please check `~/.dbt/profiles.yml` to ensure your connection configurations are valid.

Run the following command to run your models.

```bash
dbt run
```

## Example model files

```sql Define a source in dbt
{{ config(materialized='source') }}
CREATE SOURCE {{ this }} (v1 int, v2 varchar) WITH (
  connector = 'kafka',
  topic = 'kafka_1_partition_topic',
  properties.bootstrap.server = 'message_queue:29092',
  scan.startup.mode = 'earliest'
) FORMAT PLAIN ENCODE JSON
```

```sql Define a table with connector settings in dbt

{{ config(materialized='table_with_connector') }}
CREATE TABLE {{ this }} (v1 int, v2 varchar) WITH (
  connector = 'kafka',
  topic = 'kafka_1_partition_topic',
  properties.bootstrap.server = 'message_queue:29092',
  scan.startup.mode = 'earliest'
) FORMAT PLAIN ENCODE JSON

```

```sql Define a sink in dbt
{{ config(materialized='sink') }}
CREATE SINK {{ this }} AS
SELECT
   avg(distance) as avg_distance,
   avg(duration) as avg_duration
FROM taxi_trips
WITH (
   connector='kafka',
   properties.bootstrap.server='localhost:9092',
   topic='test'
)
FORMAT PLAIN ENCODE JSON;
```

```sql Define a materialized view in dbt
{{ config(materialized='materialized_view') }}
SELECT
    auction,
    to_char(date_time, 'YYYY-MM-DD') AS day,
    count(*) AS total_bids,
    count(*) filter (where price < 10000) AS rank1_bids,
    count(*) filter (where price >= 10000 and price < 1000000) AS rank2_bids,
    count(*) filter (where price >= 1000000) AS rank3_bids,
    min(price) AS min_price,
    max(price) AS max_price,
    avg(price) AS avg_price,
    sum(price) AS sum_price
FROM {{ ref('bid') }}
GROUP BY auction, to_char(date_time, 'YYYY-MM-DD')

```

```sql Define a table with indexes in dbt
{{ config(
    materialized = 'table',
    indexes=[
      {'columns': ['c1']},
      {'columns': ['c2', 'c1']},
    ]
)}}
select 1 as c1, 1 c2 union select 2 as c1, 2 as c2
```

## Zero-downtime rebuilds for materialized views and views

dbt-risingwave supports swap-based rebuilds for `materialized_view` and `view` models. For materialized views, the adapter uses [`ALTER MATERIALIZED VIEW ... SWAP WITH ...`](/sql/commands/sql-alter-materialized-view#swap-with); for views, it uses [`ALTER VIEW ... SWAP WITH ...`](/sql/commands/sql-alter-view#swap-with). This keeps the original object name resolvable throughout the rebuild. Materialized view swap requires RisingWave v2.2 or later. For the upstream reference, see the [`dbt-risingwave` zero-downtime rebuilds guide](https://github.com/risingwavelabs/dbt-risingwave/blob/main/docs/zero-downtime-rebuilds.md).

### How swap-based rebuilds work

When a model already exists and zero-downtime rebuilds are enabled, the adapter:

1. Creates a temporary object with the new definition. The temporary name follows the pattern `{original_name}_dbt_zero_down_tmp_{timestamp}`.
2. Swaps the temporary object with the original object by using `SWAP WITH`.
3. Tries to clean up the old object, which now has the temporary name.

The swap is a rename swap, not a dependency rewrite. After the swap, the original name points to the new object ID, while downstream dependencies still point to the pre-swap object ID until those downstream models are rebuilt.

```text
Before swap

name `orders_mv` ---------------------------------> object id=A (old definition)
name `orders_mv_dbt_zero_down_tmp_<timestamp>` ---> object id=B (new definition)
downstream `daily_orders_mv` ---------------------> depends on object id=A

ALTER MATERIALIZED VIEW orders_mv
SWAP WITH orders_mv_dbt_zero_down_tmp_<timestamp>;

After swap

name `orders_mv` ---------------------------------> object id=B (new definition)
name `orders_mv_dbt_zero_down_tmp_<timestamp>` ---> object id=A (old definition)
downstream `daily_orders_mv` ---------------------> still depends on object id=A
```

If you set `immediate_cleanup` to `true`, the adapter attempts to drop the temporary object immediately after the swap. This cleanup is best-effort and dependency-safe: the adapter checks for remaining dependents and preserves the temporary object if anything still references it. It does not use `CASCADE` for zero-downtime cleanup.

For a chain such as `mv1 -> mv2 -> mv3`, rebuilding only `mv1` can leave `mv2` and `mv3` attached to the preserved temporary `mv1` object. Rebuild dependent models before cleanup:

```bash
dbt run --select "mv1+" --vars 'zero_downtime: true'
dbt run-operation cleanup_temp_objects
```

### When swap-based rebuilds apply

dbt-risingwave uses the zero-downtime swap path only when all of the following are true:

1. The relation already exists.
2. The run is not using `--full-refresh`.
3. The model config sets `zero_downtime={'enabled': true}`.
4. The command includes `--vars 'zero_downtime: true'`.

Otherwise, dbt-risingwave falls back to the standard rebuild path.

### Configuration

You must enable zero downtime in both the model config and the CLI invocation.

```sql Materialized view
{{ config(
    materialized='materialized_view',
    zero_downtime={'enabled': true}
) }}

select *
from {{ ref('source_table') }}
```

```sql View
{{ config(
    materialized='view',
    zero_downtime={'enabled': true}
) }}

select *
from {{ ref('source_table') }}
```

```bash
dbt run --vars 'zero_downtime: true'
```

To attempt cleanup immediately after the swap, set `immediate_cleanup` in the model config:

```sql
{{ config(
    materialized='materialized_view',
    zero_downtime={'enabled': true, 'immediate_cleanup': true}
) }}
```

### Cleanup helpers

dbt-risingwave includes helper macros for finding and cleaning up preserved temporary objects:

```bash
dbt run-operation list_temp_objects
dbt run-operation cleanup_temp_objects --args '{"dry": true}'
dbt run-operation cleanup_temp_objects
```

You can scope these helpers to a schema:

```bash
dbt run-operation list_temp_objects --args '{"schema_name": "public"}'
dbt run-operation cleanup_temp_objects --args '{"schema_name": "public"}'
```

### Source and test references

The behavior above is documented and exercised in the upstream `dbt-risingwave` project:

- Canonical explainer: [`docs/zero-downtime-rebuilds.md`](https://github.com/risingwavelabs/dbt-risingwave/blob/main/docs/zero-downtime-rebuilds.md)
- Materialized view swap logic: [`dbt/include/risingwave/macros/materializations/materialized_view.sql`](https://github.com/risingwavelabs/dbt-risingwave/blob/main/dbt/include/risingwave/macros/materializations/materialized_view.sql)
- View swap logic: [`dbt/include/risingwave/macros/materializations/view.sql`](https://github.com/risingwavelabs/dbt-risingwave/blob/main/dbt/include/risingwave/macros/materializations/view.sql)
- Cleanup and helper macros: [`dbt/include/risingwave/macros/adapters.sql`](https://github.com/risingwavelabs/dbt-risingwave/blob/main/dbt/include/risingwave/macros/adapters.sql)
- End-to-end zero-downtime models and assertions: [`tests/e2e/zero_downtime`](https://github.com/risingwavelabs/dbt-risingwave/tree/main/tests/e2e/zero_downtime) and [`tests/e2e/zero_downtime/tests/assert_zero_downtime_materializations.sql`](https://github.com/risingwavelabs/dbt-risingwave/blob/main/tests/e2e/zero_downtime/tests/assert_zero_downtime_materializations.sql)
- CI workflow that runs the swap path and cleanup helper: [`.github/workflows/ci.yml`](https://github.com/risingwavelabs/dbt-risingwave/blob/main/.github/workflows/ci.yml)

## Additional resources

For details about building and deploying dbt models in general, please refer to the [dbt documentation](https://docs.getdbt.com/docs/introduction).
